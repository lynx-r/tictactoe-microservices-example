encrypt.key: ${ENCRYPT_KEY:12345}

spring:
  application:
    name: userservice
  profiles:
    active:
      default
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configserver
